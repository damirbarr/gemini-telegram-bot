# Telegram Bot Credentials
TELEGRAM_BOT_TOKEN="your_bot_token_here_from_botfather"
TELEGRAM_ALLOWED_USER_ID="123456789" # Your numeric ID from @userinfobot

# Gemini CLI Path (Default is 'gemini' if it's in your system PATH)
# If installed via NVM or globally differently, point directly to it.
GEMINI_PATH="gemini"
